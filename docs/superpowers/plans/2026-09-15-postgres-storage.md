# 网关存储单一化到 PostgreSQL — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把网关自身的元数据库从「dev=SQLite / prod=MySQL」双轨合并为单一 PostgreSQL。

**Architecture:** 删除 GORM AutoMigrate 整条路径，让 `migrations/*.sql` 成为唯一
schema 权威；44 个 MySQL 迁移压成一份 PG baseline；测试改为对真实 PG 按 schema 隔离
运行。被纳管的目标库驱动（MySQL/TiDB/Oracle/SQLite/GaussDB）完全不动。

**Tech Stack:** Go 1.23 · GORM 1.25.12 · `gorm.io/driver/postgres`(pgx) ·
PostgreSQL 16.14 (Homebrew, 本机) · Gin

**Spec:** `docs/superpowers/specs/2026-09-15-postgres-migration-design.md`

## Global Constraints

- module 名 `velagateway`，Go 1.23，工作目录 `backend/`
- 本机 PG：`127.0.0.1:5432`，登录角色 `abiu`（超级用户，免密），服务端 16.14
- 运行库 `vela_gateway`，测试库 `vela_test`（已存在）
- 默认 DSN 不写死用户名：`host=127.0.0.1 port=5432 dbname=vela_gateway sslmode=disable`
- 测试 DSN 环境变量 `VELA_TEST_PG_DSN`，运行 DSN 环境变量 `VELA_PG_DSN`
- 表命名 `tbl_`、主键 `pk_`、索引 `idx_`；**无物理外键**（backend 文档 §9）
- **禁止改动** `internal/gateway/`（realdb.go 及其全部测试）、`internal/service/exec_batch_test.go`、
  `docs/MySQL規範.md`、`docs/TIDB規範.md`、`docs/DWS对象设计和业务开发规范-ABLE.md`、
  `backend/target.db` —— 这些属于「被纳管的目标库」，与网关自身存储无关
- 每个任务结束提交一次，提交信息用英文

---

### Task 1: PostgreSQL baseline schema

把 0001–0044 累积后的最终形态写成一份 PG baseline。这是整个计划的地基，
后续所有任务都依赖它建出的表。

**Files:**
- Modify: `backend/migrations/0001_init.sql`（整体重写）
- Delete: `backend/migrations/0002_*.sql` … `backend/migrations/0044_*.sql`（43 个）
- Test: 用 psql 对临时库直接验证（此任务不涉及 Go 代码）

**Interfaces:**
- Consumes: 无
- Produces: 36 张 `tbl_*` 表的 PG DDL。后续任务依赖的表名与列名以此文件为准。

- [ ] **Step 1: 导出现有 schema 作为交叉验证基准**

`allModels` 声称与 `migrations/*.sql` 同步。用旧的 SQLite 路径导出一份，
作为推导 baseline 时的对照——两边对不上的地方就是历史漂移，需要人工裁决，
以 SQL 迁移为权威（它才是生产 schema）。

```bash
cd backend
git stash list >/dev/null  # 确认工作区干净
go run ./cmd/server migrate --config configs/config.yaml 2>/dev/null || true
sqlite3 vela-gateway.db .schema > /tmp/claude-501/sqlite-schema.txt
wc -l /tmp/claude-501/sqlite-schema.txt
```

- [ ] **Step 2: 逐个读 44 个迁移文件，推导每张表的最终形态**

权威来源是 SQL 文件本身。0001 建 36 张表，其后是 66 条 `ALTER TABLE`、
5 条 `CREATE INDEX`、12 条 `UPDATE`、6 条 `INSERT`。

```bash
cd backend
for f in migrations/00*.sql; do echo "===== $f ====="; cat "$f"; done > /tmp/claude-501/all-migrations.sql
grep -c "" /tmp/claude-501/all-migrations.sql
```

注意 0038/0039/0040/0041/0042/0043/0044 是列改名，最终列名以它们为准
（例如 `api_client.key` → `api_key`，见 `repository/apiclient.go:30` 的注释）。
12 条 `UPDATE` 和 6 条 `INSERT` 是数据回填，**不进 baseline**——Go 侧的
`backfillGliEnv` / `backfillEnvTiers` / `seedPipelineReference` /
`backfillApprovalExecuted` / `backfillStrictNoWhere` 已经幂等地覆盖了它们。

- [ ] **Step 3: 写 baseline**

按下表逐列翻译。文件开头保留说明注释（改为描述 PG）。

| MySQL | PostgreSQL |
|---|---|
| `BIGINT NOT NULL AUTO_INCREMENT` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` |
| `TINYINT(1)` | `BOOLEAN` |
| `DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)` | `TIMESTAMPTZ NOT NULL DEFAULT now()` |
| `MEDIUMTEXT` / `LONGTEXT` / `TEXT` | `TEXT` |
| `CHAR(64)` | `VARCHAR(64)` |
| 表内 `UNIQUE KEY x (...)` | 表外 `CREATE UNIQUE INDEX x ON t (...);` |
| 表内 `KEY x (...)` | 表外 `CREATE INDEX x ON t (...);` |
| `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4` | 删除 |
| `CREATE DATABASE IF NOT EXISTS` / `USE vela_gateway` | 删除 |

`CHAR(64)` 必须换 `VARCHAR(64)`：PG 的 `char(n)` 是 blank-padded，读回 Go 带尾部
空格，而 `audit_chain.go:142` 用 Go 侧字符串判等比 `prev_hash`。

email 唯一索引建在 `lower()` 上（Task 5 依赖此索引）：

```sql
CREATE UNIQUE INDEX idx_user_email ON tbl_user (lower(email));
```

表头示例（`tbl_role`，其余 35 张同法）：

```sql
CREATE TABLE IF NOT EXISTS tbl_role (
  id                BIGINT       GENERATED BY DEFAULT AS IDENTITY,
  code              VARCHAR(64)  NOT NULL,
  name              VARCHAR(64)  NOT NULL,
  layer             VARCHAR(32)  NOT NULL,
  description       VARCHAR(255),
  default_conn_role VARCHAR(64),
  can_approve       BOOLEAN      NOT NULL DEFAULT false,
  icon              VARCHAR(32),
  created_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),
  CONSTRAINT pk_tbl_role PRIMARY KEY (id)
);
CREATE UNIQUE INDEX idx_role_code ON tbl_role (code);
```

- [ ] **Step 4: 删除 0002–0044**

```bash
cd backend && git rm migrations/00{02,03,04,05,06,07,08,09}_*.sql migrations/0[1-4][0-9]_*.sql
ls migrations/
```
Expected: 只剩 `0001_init.sql` 和 `embed.go`

- [ ] **Step 5: 对临时库验证 baseline 可执行**

```bash
createdb vela_baseline_check
psql -v ON_ERROR_STOP=1 -d vela_baseline_check -f backend/migrations/0001_init.sql
psql -d vela_baseline_check -Atc "select count(*) from information_schema.tables where table_schema='public' and table_name like 'tbl_%';"
```
Expected: 无报错；表数输出 `36`

- [ ] **Step 6: 与 Step 1 的对照基准比表名**

```bash
psql -d vela_baseline_check -Atc "select table_name from information_schema.tables where table_schema='public' and table_name like 'tbl_%' order by 1;" > /tmp/claude-501/pg-tables.txt
grep -oE "tbl_[a-z_]+" /tmp/claude-501/sqlite-schema.txt | sort -u > /tmp/claude-501/sqlite-tables.txt
diff /tmp/claude-501/pg-tables.txt /tmp/claude-501/sqlite-tables.txt
```
Expected: 无差异。有差异则逐个裁决，**以 SQL 迁移为准**并在提交信息里记录原因。

- [ ] **Step 7: 清理并提交**

```bash
dropdb vela_baseline_check
cd backend && git add migrations/
git commit -m "feat(schema): collapse 44 MySQL migrations into one PostgreSQL baseline"
```

---

### Task 2: 连接层切到 PostgreSQL，并立起测试底座

本任务一次做完驱动切换、配置字段替换、所有引用点清理和 `testsupport` 包。
它们不能拆：删掉 `Config.Database.Driver` / `SQLitePath` / `MySQLDSN` /
`AutoMigrate` 四个字段，会同时打断 `main.go`（5 处）、`migrate.go`（1 处）和
7 个 bootstrap 测试文件；而那 7 个测试要改成用 `testsupport` 建库，`testsupport`
又要等 postgres 驱动就位。中间任何一个切点都是编译不过的状态，不值得单独验收。

**Files:**
- Modify: `backend/go.mod`, `backend/go.sum`
- Modify: `backend/internal/bootstrap/db.go`
- Modify: `backend/internal/bootstrap/config.go`（Database 结构、env 覆盖、driver 推导）
- Modify: `backend/internal/bootstrap/migrate.go`（`Migrate` 里的 driver 分支）
- Modify: `backend/cmd/server/main.go`（3 处 driver 日志、2 处 `AutoMigrate = false`）
- Modify: `backend/configs/config.yaml`, `backend/configs/config.prod.yaml`
- Create: `backend/internal/testsupport/db.go`
- Create: `backend/internal/testsupport/db_test.go`
- Modify（7 个直接给 `cfg.Database.*` 赋值的测试）：
  - `backend/internal/bootstrap/harness_test.go:37-60`
  - `backend/internal/bootstrap/init_test.go`
  - `backend/internal/bootstrap/seed_backfill_test.go`
  - `backend/internal/bootstrap/envtier_menu_backfill_test.go`
  - `backend/internal/bootstrap/config_hardening_test.go`
  - `backend/internal/bootstrap/audit_fork_test.go`
  - `backend/internal/bootstrap/tier_column_rename_test.go`
- Modify: `backend/internal/bootstrap/migrate_test.go:18`（直接 import sqlite，依赖被移除后编译不过）
- Modify: `backend/internal/bootstrap/config_test.go`

**Interfaces:**
- Consumes: Task 1 的 baseline（`migrations.FS` 中的 `0001_init.sql`）
- Produces:
  - `Config.Database.PostgresDSN string`（yaml `postgres_dsn`）——
    `Driver` / `MySQLDSN` / `SQLitePath` / `AutoMigrate` 四个字段删除
  - `OpenDB(cfg *Config) (*gorm.DB, error)`，签名不变，但不再建表
  - `testsupport.NewDB(t *testing.T) *gorm.DB`、`testsupport.DSN() string`

**⚠️ 依赖方向**：`testsupport` **不得** import `velagateway/internal/bootstrap`。
`internal/bootstrap` 的测试是内部测试（`package bootstrap`），它们要 import
`testsupport`；反向再 import 就是循环导入。`testsupport` 只依赖
`velagateway/migrations`。

- [ ] **Step 1: 换依赖**

```bash
cd backend
go get gorm.io/driver/postgres
go mod edit -droprequire gorm.io/driver/mysql
go mod edit -droprequire github.com/glebarez/sqlite
go mod tidy
grep -E "postgres|driver/mysql|glebarez" go.mod
```
Expected: 出现 `gorm.io/driver/postgres`；`gorm.io/driver/mysql` 与
`github.com/glebarez/sqlite` 消失；`github.com/glebarez/go-sqlite`、
`github.com/go-sql-driver/mysql`、`github.com/lib/pq`、`github.com/sijms/go-ora/v2`
**必须保留**（realdb.go 连目标库用）。

此刻 `go build ./...` 会失败，Step 2–7 把它修回来。

- [ ] **Step 2: 写失败的测试（testsupport）**

`backend/internal/testsupport/db_test.go`:

```go
package testsupport

import "testing"

// 两个库互不可见:各自建一行,都只看得到自己那一行。
func TestNewDB_SchemasAreIsolated(t *testing.T) {
	a := NewDB(t)
	b := NewDB(t)

	if err := a.Exec(`INSERT INTO tbl_role (code, name, layer) VALUES ('only_in_a', 'A', 'l')`).Error; err != nil {
		t.Fatalf("insert into a: %v", err)
	}

	var inB int64
	if err := b.Raw(`SELECT count(*) FROM tbl_role WHERE code = 'only_in_a'`).Scan(&inB).Error; err != nil {
		t.Fatalf("count in b: %v", err)
	}
	if inB != 0 {
		t.Fatalf("b 看到了 a 的行:%d,schema 隔离失效", inB)
	}
}

// baseline 真的跑过了:36 张表都在。
func TestNewDB_RunsBaseline(t *testing.T) {
	db := NewDB(t)
	var n int64
	err := db.Raw(`SELECT count(*) FROM information_schema.tables
	               WHERE table_schema = current_schema() AND table_name LIKE 'tbl_%'`).Scan(&n).Error
	if err != nil {
		t.Fatalf("count tables: %v", err)
	}
	if n != 36 {
		t.Fatalf("表数 = %d, want 36", n)
	}
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd backend && go test ./internal/testsupport/ -v`
Expected: 编译失败，`undefined: NewDB`

- [ ] **Step 4: 实现 testsupport**

`backend/internal/testsupport/db.go`:

```go
// Package testsupport 给测试提供一个真实 PostgreSQL 上的独占 schema。
//
// 隔离用 schema 而不是 database:CREATE DATABASE 在 PG 上要复制模板库,几百个
// 测试各建一个会把测试时间拖成分钟级;schema 的创建近乎免费,且允许并行。
package testsupport

import (
	"fmt"
	"io/fs"
	"os"
	"strings"
	"sync/atomic"
	"testing"

	"gorm.io/driver/postgres"
	"gorm.io/gorm"
	gormlogger "gorm.io/gorm/logger"

	"velagateway/migrations"
)

// defaultDSN 指向本机的测试库。CI 或换机器时用 VELA_TEST_PG_DSN 覆盖。
const defaultDSN = "host=127.0.0.1 port=5432 dbname=vela_test sslmode=disable"

var seq atomic.Int64

// DSN 返回测试库连接串。
func DSN() string {
	if v := os.Getenv("VELA_TEST_PG_DSN"); v != "" {
		return v
	}
	return defaultDSN
}

// NewDB 建一个独占 schema,在其中建好全部表,返回绑定到它的 *gorm.DB。
// 测试结束时 schema 连同其中的表一起删掉。
//
// 连不上就让测试失败,不 Skip:静默跳过的测试等于假绿,而这套测试的全部价值
// 就在于它们真的对 PostgreSQL 跑过。
func NewDB(t *testing.T) *gorm.DB {
	t.Helper()

	dsn := DSN()
	admin, err := gorm.Open(postgres.Open(dsn), &gorm.Config{
		Logger: gormlogger.Default.LogMode(gormlogger.Silent),
	})
	if err != nil {
		t.Fatalf("连不上测试库 (%s):%v\n建库:createdb vela_test", dsn, err)
	}

	schema := fmt.Sprintf("t_%d_%d", os.Getpid(), seq.Add(1))
	if err := admin.Exec(`CREATE SCHEMA ` + schema).Error; err != nil {
		t.Fatalf("create schema %s: %v", schema, err)
	}

	// search_path 要写进 DSN 重新连一次 —— 池里的每条连接都得带上它,否则第二条
	// 连接落回 public,建出来的表就消失在另一个 schema 里。
	scopedDSN := dsn + " search_path=" + schema
	if strings.Contains(dsn, "://") {
		sep := "&"
		if !strings.Contains(dsn, "?") {
			sep = "?"
		}
		scopedDSN = dsn + sep + "search_path=" + schema
	}

	scoped, err := gorm.Open(postgres.Open(scopedDSN), &gorm.Config{
		Logger: gormlogger.Default.LogMode(gormlogger.Silent),
	})
	if err != nil {
		t.Fatalf("连 schema %s: %v", schema, err)
	}

	// 直接执行 baseline,不走 bootstrap.RunSQLMigrations。两个理由:
	//
	// 1. bootstrap 的测试是内部测试(package bootstrap),它们要 import 这个包;
	//    这个包再 import bootstrap 就成了循环导入,编译不过。
	// 2. RunSQLMigrations 会抢 advisory lock。测试库里每个 schema 都是独占的,
	//    没有并发迁移可言,而那把锁会把所有测试的建库串成一条队。
	sqlBytes, err := fs.ReadFile(migrations.FS, "0001_init.sql")
	if err != nil {
		t.Fatalf("read baseline: %v", err)
	}
	if err := scoped.Exec(string(sqlBytes)).Error; err != nil {
		t.Fatalf("baseline 建表失败 (schema %s):%v", schema, err)
	}

	t.Cleanup(func() {
		if sqlDB, derr := scoped.DB(); derr == nil {
			_ = sqlDB.Close()
		}
		if err := admin.Exec(`DROP SCHEMA ` + schema + ` CASCADE`).Error; err != nil {
			t.Logf("清理 schema %s 失败:%v", schema, err)
		}
		if sqlDB, derr := admin.DB(); derr == nil {
			_ = sqlDB.Close()
		}
	})

	return scoped
}
```

- [ ] **Step 5: 改 config.go**

`Database` 结构换成：

```go
	Database struct {
		// PostgresDSN is the gateway's own store. libpq key=value or URL form;
		// omitting user= lets libpq fall back to the OS user.
		PostgresDSN string `yaml:"postgres_dsn"`
		Seed        bool   `yaml:"seed"`
	} `yaml:"database"`
```

env 覆盖：`VELA_MYSQL_DSN` 那段换成

```go
	if v := os.Getenv("VELA_PG_DSN"); v != "" {
		cfg.Database.PostgresDSN = v
	}
```

驱动推导整段删除（`VELA_DB_DRIVER` 与 `cfg.Env == "prod"` 分支）。`cfg.Env` 的
解析与 `isKnownEnv` 警告保留，警告文案里「使用 SQLite」去掉：

```go
		slog.Warn("未识别的环境名,将按 dev 处理", "env", rawEnv)
```

- [ ] **Step 6: 改 db.go**

`OpenDB` 的三分支 switch 换成单一 postgres 路径：

```go
// OpenDB connects to the gateway's own PostgreSQL store. It does NOT create
// tables — schema is Migrate's job, and it is the only one that has it.
func OpenDB(cfg *Config) (*gorm.DB, error) {
	gcfg := &gorm.Config{Logger: gormlogger.Default.LogMode(gormlogger.Warn)}

	db, err := gorm.Open(postgres.Open(cfg.Database.PostgresDSN), gcfg)
	if err != nil {
		return nil, fmt.Errorf("connect postgres (库是否已建? `createdb vela_gateway`): %w", err)
	}
	return db, nil
}
```

导入改为 `"gorm.io/driver/postgres"`，删掉 `mysql`、`sqlite`、`strings` 的导入。
`SetMaxOpenConns(1)` 删除——那是 SQLite 单写者的限制。

`allModels` 与 `shouldAutoMigrate` 本任务先留着（Task 3 删），但 `OpenDB` 不再
调用它们；`model` 的导入因此仍然需要。

- [ ] **Step 7: 改 migrate.go 与 main.go 的引用点**

`migrate.go` 的 `Migrate` 里：

```go
	if cfg.Database.Driver == "mysql" {
		if err := RunSQLMigrations(db, migrations.FS); err != nil {
			return err
		}
	} else if err := autoMigrate(db); err != nil {
		return err
	}
```

换成

```go
	if err := RunSQLMigrations(db, migrations.FS); err != nil {
		return err
	}
```

`main.go`：3 处日志里的 `"driver", cfg.Database.Driver` 参数删掉；2 处
`cfg.Database.AutoMigrate = false` 整行删掉（字段没了，且 `OpenDB` 本来就不再建表）。

- [ ] **Step 8: 改 bootstrap 包里的建库代码（7 个设 cfg 的 + migrate_test.go）**

每个文件里形如

```go
	cfg.Database.Driver = "sqlite"
	cfg.Database.SQLitePath = filepath.Join(t.TempDir(), "xxx.db")
	cfg.Database.AutoMigrate = true
	db, err := OpenDB(cfg)
	if err != nil {
		t.Fatalf("OpenDB: %v", err)
	}
```

换成

```go
	db := testsupport.NewDB(t)
```

`cfg` 本身仍要构造（后续代码读 `cfg.Gateway`、`cfg.JWT` 等），只是不再由它决定
建库。导入加 `"velagateway/internal/testsupport"`，删掉因此不再使用的
`filepath` / `sqlite` / `gorm` / `gormlogger`。

`migrate_test.go:18` 不设 `cfg.Database.*`，而是直接
`gorm.Open(sqlite.Open(path))`——它 import 了 `glebarez/sqlite`，Step 1 移除该依赖
后这个包的测试就编译不过，所以必须在本任务一并改掉，同样换成 `testsupport.NewDB(t)`。

`tier_column_rename_test.go` 有两处 `gorm.Open(sqlite.Open(cfg.Database.SQLitePath))`
（:56 和 :149）——它测的是列改名迁移，baseline 压缩后这个测试已无对象可测，
改为断言 baseline 建出的就是改名后的列：

```go
// 0038–0044 那批列改名已经折进 baseline。这里守的是结果:改名后的列在,
// 旧名字不在。列名一旦回退,repository 里按新名写的查询会静默查不到东西。
func TestBaselineUsesRenamedColumns(t *testing.T) {
	db := testsupport.NewDB(t)
	for _, c := range []struct{ table, col string }{
		{"tbl_api_client", "api_key"},
		{"tbl_env_tier", "tier_code"},
	} {
		var n int64
		err := db.Raw(`SELECT count(*) FROM information_schema.columns
		               WHERE table_schema = current_schema() AND table_name = ? AND column_name = ?`,
			c.table, c.col).Scan(&n).Error
		if err != nil {
			t.Fatalf("query columns: %v", err)
		}
		if n != 1 {
			t.Errorf("%s.%s 不存在", c.table, c.col)
		}
	}
}
```

`tbl_env_tier.tier_code` 与 `tbl_api_client.api_key` 的实际列名以 Task 1 产出的
baseline 为准；若 baseline 里不是这两个名字，按 baseline 改断言，不要反过来改 baseline。

- [ ] **Step 9: 改 config_test.go**

`config_test.go:11` 的 `writeTempConfig(t, envLine)` 拼出的 yaml 里还写着
`mysql_dsn` / `sqlite_path` / `auto_migrate`，把 `database` 段换掉：

```go
func writeTempConfig(t *testing.T, envLine string) string {
	t.Helper()
	body := envLine + `
database:
  postgres_dsn: "host=127.0.0.1 port=5432 dbname=vela_gateway sslmode=disable"
  seed: false
jwt:
  secret: "test-strong-secret-0123456789abcdef-xyz"
`
	p := filepath.Join(t.TempDir(), "config.yaml")
	if err := os.WriteFile(p, []byte(body), 0o600); err != nil {
		t.Fatalf("write temp config: %v", err)
	}
	return p
}
```

`config_test.go:83` 的 `TestLoadConfigDriverResolution` 整体换掉——它断言 7 种组合下
的 `cfg.Database.Driver`，而该字段已不存在。保留其中仍有意义的那一半，env 的解析与别名：

```go
// env 仍然要解析(它决定 seed / JWT 强度校验 / CORS),但不再决定存储驱动 ——
// 存储只有 PostgreSQL 一种,VELA_DB_DRIVER 这个开关连同它的歧义一起没了。
func TestLoadConfigEnvResolution(t *testing.T) {
	cases := []struct {
		name    string
		envLine string // yaml `env:` line (may be empty)
		appEnv  string
		velaEnv string
		wantEnv string
	}{
		{"default is dev", "", "", "", "dev"},
		{"yaml env:prod", `env: "prod"`, "", "", "prod"},
		{"APP_ENV=prod overrides yaml dev", `env: "dev"`, "prod", "", "prod"},
		{"APP_ENV=production alias", "", "production", "", "prod"},
		{"VELA_ENV=prod when APP_ENV empty", "", "", "prod", "prod"},
		{"unknown env falls back to dev", "", "staging", "", "dev"},
	}
	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
			t.Setenv("APP_ENV", tc.appEnv)
			t.Setenv("VELA_ENV", tc.velaEnv)

			cfg, err := LoadConfig(writeTempConfig(t, tc.envLine))
			if err != nil {
				t.Fatalf("LoadConfig: %v", err)
			}
			if cfg.Env != tc.wantEnv {
				t.Errorf("Env = %q, want %q", cfg.Env, tc.wantEnv)
			}
			if cfg.Database.PostgresDSN == "" {
				t.Error("postgres_dsn 未读入")
			}
		})
	}
}

func TestLoadConfig_EnvOverridesDSN(t *testing.T) {
	t.Setenv("VELA_PG_DSN", "host=127.0.0.1 dbname=from_env sslmode=disable")
	cfg, err := LoadConfig(writeTempConfig(t, `env: "dev"`))
	if err != nil {
		t.Fatalf("LoadConfig: %v", err)
	}
	if !strings.Contains(cfg.Database.PostgresDSN, "from_env") {
		t.Fatalf("VELA_PG_DSN 未覆盖: %q", cfg.Database.PostgresDSN)
	}
}
```

导入里加 `"strings"`。

- [ ] **Step 10: 改两个 config yaml**

`configs/config.yaml` 的 `database` 段：

```yaml
# Storage — the gateway's own metadata store. PostgreSQL only.
# Create it once with:  createdb vela_gateway
# Override at runtime with VELA_PG_DSN.
database:
  # Omitting user= lets libpq fall back to the OS user.
  postgres_dsn: "host=127.0.0.1 port=5432 dbname=vela_gateway sslmode=disable"
  seed: true     # insert demo data when tables are empty
```

文件顶部 `env:` 上方那段「dev → SQLite / prod → MySQL」的注释改写为：env 只决定
seed / JWT 强度 / CORS，不再决定存储。`config.prod.yaml` 同法（DSN 留空并注明
必须由 `VELA_PG_DSN` 提供）。

- [ ] **Step 11: 编译并跑 testsupport 自测**

```bash
cd backend
go build ./... && echo BUILD_OK
createdb vela_test 2>/dev/null || true
go test ./internal/testsupport/ -v
```
Expected: `BUILD_OK`；两个用例 PASS

- [ ] **Step 12: 跑 bootstrap 包，记录失败基线**

```bash
cd backend && go test ./internal/bootstrap/ 2>&1 | tail -40
```
Expected: 编译通过。部分用例仍失败——Task 3/5/6 依次修复。把此刻的失败清单写进
提交信息，作为后续任务的验收基线。

**此时 `go test ./...` 整体仍编译不过**，这是预期的：`internal/repository` 下还有
10 个测试 import `glebarez/sqlite`，Task 4 修。本步只跑 bootstrap 这一个包。

- [ ] **Step 13: 提交**

```bash
cd backend && git add go.mod go.sum internal/ cmd/server/main.go configs/
git commit -m "feat(store): open the gateway store with postgres, add a test harness"
```

---

### Task 3: 删除 AutoMigrate 路径，迁移锁换成 PG advisory lock

**Files:**
- Modify: `backend/internal/bootstrap/migrate.go:35-42`（Migrate）、`:73-` (autoMigrate)、`:162-203`（迁移锁）
- Modify: `backend/internal/bootstrap/db.go`（删 `allModels`、`shouldAutoMigrate`）
- Modify: `backend/internal/bootstrap/config.go`（删 `Database.AutoMigrate` 字段）
- Modify: `backend/configs/config.yaml`, `backend/configs/config.prod.yaml`（删 `auto_migrate`）
- Modify: `backend/cmd/server/main.go:58`（serve 路径补 Migrate）
- Modify: `backend/internal/bootstrap/automigrate_guard_test.go`（改写断言）
- Test: `backend/internal/bootstrap/migrate_test.go`

**Interfaces:**
- Consumes: `testsupport.NewDB(t)`
- Produces: `Migrate(cfg *Config, db *gorm.DB) error` —— 唯一的 schema 入口，
  签名不变；`autoMigrate`、`allModels`、`shouldAutoMigrate` 不再存在

- [ ] **Step 1: 写失败的测试**

`automigrate_guard_test.go` 整体改写。原来它守的是「MySQL 不许 AutoMigrate」，
现在守的是「AutoMigrate 这条路根本不存在」：

```go
// schema 只有一个权威来源:migrations/*.sql。
//
// 从前不是这样 —— dev 走 GORM AutoMigrate,prod 走 SQL 迁移,两份 schema 各自演进。
// db.go 里记着那次事故:tier_code 改名只做在一边,AutoMigrate 在已填充的 env 旁边
// 建了个空的 tier_code,而空值在两处查询里都读作「放行」,网关带着失效的管控上线,
// 健康检查全绿。这个测试是那条路被拆掉之后留下的钉子。
func TestSchemaHasOneSourceOfTruth(t *testing.T) {
	src, err := os.ReadFile("db.go")
	if err != nil {
		t.Fatalf("read db.go: %v", err)
	}
	for _, banned := range []string{"allModels", "autoMigrate", "shouldAutoMigrate"} {
		if bytes.Contains(src, []byte(banned)) {
			t.Errorf("db.go 又出现了 %q:schema 只能由 migrations/*.sql 定义", banned)
		}
	}
}

// Migrate 跑完,36 张表都在。
func TestMigrate_CreatesEverySchemaTable(t *testing.T) {
	db := testsupport.NewDB(t)  // NewDB 已经跑过 baseline
	var n int64
	err := db.Raw(`SELECT count(*) FROM information_schema.tables
	               WHERE table_schema = current_schema() AND table_name LIKE 'tbl_%'`).Scan(&n).Error
	if err != nil {
		t.Fatalf("count: %v", err)
	}
	if n != 36 {
		t.Fatalf("表数 = %d, want 36", n)
	}
}

// 跑第二次不炸 —— 账本挡住已应用的版本。
func TestMigrate_IsIdempotent(t *testing.T) {
	db := testsupport.NewDB(t)
	cfg := &Config{}
	cfg.Gateway.StrictMode = true
	if err := Migrate(cfg, db); err != nil {
		t.Fatalf("第二次 Migrate: %v", err)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && go test ./internal/bootstrap/ -run "TestSchemaHasOneSourceOfTruth|TestMigrate_" -v`
Expected: `TestSchemaHasOneSourceOfTruth` FAIL（db.go 里还有 allModels）

- [ ] **Step 3: 删除 AutoMigrate 路径**

`db.go`：删掉 `allModels` 变量与 `shouldAutoMigrate` 函数（`OpenDB` 在 Task 2
已经不再调用它们）。`migrate.go`：删掉 `autoMigrate` 函数，`Migrate` 改为

```go
func Migrate(cfg *Config, db *gorm.DB) error {
	if err := RunSQLMigrations(db, migrations.FS); err != nil {
		return err
	}
	// 以下回填的注释与理由保持原样不动。
	if err := backfillGliEnv(db); err != nil {
		return err
	}
	...
}
```

`Migrate` 上方那段「MySQL / SQLite 两条路」的文档注释改写为单一 PG 路径。

`config.go` 删 `Database.AutoMigrate` 字段，两个 yaml 删 `auto_migrate:` 行。

- [ ] **Step 4: serve 路径补上 Migrate**

`main.go:58` 附近。此前 serve 靠 `OpenDB` 内部的 AutoMigrate 建表，现在必须显式调：

```go
	db, err := bootstrap.OpenDB(cfg)
	if err != nil {
		slog.Error("database init failed", "err", err)
		os.Exit(1)
	}
	// 此前这一步藏在 OpenDB 的 AutoMigrate 分支里,只有 dev 走到。现在 serve /
	// migrate / init 三条路都经过同一个 Migrate —— 顺带修好了一个旧缺陷:dev 的
	// serve 路径从来没跑过 backfillGliEnv / backfillEnvTiers / seedPipelineReference /
	// backfillApprovalExecuted 这四个回填,它们此前只挂在 migrate 子命令上。
	if err := bootstrap.Migrate(cfg, db); err != nil {
		slog.Error("schema migration failed", "err", err)
		os.Exit(1)
	}
```

- [ ] **Step 5: 迁移锁换成 PG advisory lock**

`migrate.go:167` 的 `if db.Dialector.Name() == "mysql"` 整块改为 postgres 版本。
「必须 pin 同一条 `*sql.Conn` 端到端持有」的推理与注释**原样保留**——PG advisory
lock 同样是 session 级，那段理由一字不差地成立。只换 SQL：

```go
	// 用 try 版本而不是 pg_advisory_lock:后者无限阻塞,会把「另一个迁移正在跑」
	// 变成一次没有任何输出的挂起。try 立即返回 boolean,外面包一个有上限的重试,
	// 保住 MySQL 版 GET_LOCK(..., 60) 的等待语义。
	const lockSQL = `SELECT pg_try_advisory_lock(hashtext('vela_schema_migrate')::bigint)`
	deadline := time.Now().Add(60 * time.Second)
	var got bool
	for {
		if err := conn.QueryRowContext(ctx, lockSQL).Scan(&got); err != nil {
			return fmt.Errorf("acquire migration lock: %w", err)
		}
		if got {
			break
		}
		if time.Now().After(deadline) {
			return fmt.Errorf("could not acquire migration lock (another migration is running)")
		}
		time.Sleep(500 * time.Millisecond)
	}
	defer func() {
		var released bool
		if err := conn.QueryRowContext(ctx,
			`SELECT pg_advisory_unlock(hashtext('vela_schema_migrate')::bigint)`).Scan(&released); err != nil {
			slog.Warn("release migration lock failed", "err", err)
		} else if !released {
			slog.Warn("migration lock not held at release (connection may have dropped)")
		}
	}()
```

外层的 `if db.Dialector.Name() == "mysql"` 条件去掉——现在只有一种方言，锁总是要加。

- [ ] **Step 6: 运行测试确认通过**

```bash
cd backend && go test ./internal/bootstrap/ -run "TestSchemaHasOneSourceOfTruth|TestMigrate_" -v
go build ./... && echo BUILD_OK
```
Expected: 三个用例 PASS；`BUILD_OK`

- [ ] **Step 7: 端到端验证一次真实启动**

```bash
cd backend
createdb vela_gateway 2>/dev/null || true
go run ./cmd/server migrate --config configs/config.yaml
psql -d vela_gateway -Atc "select count(*) from information_schema.tables where table_schema='public' and table_name like 'tbl_%';"
psql -d vela_gateway -Atc "select count(*) from schema_migrations;"
```
Expected: 表数 `36`；账本 `1` 行

- [ ] **Step 8: 提交**

```bash
cd backend && git add -A internal/bootstrap cmd/server/main.go configs/
git commit -m "refactor(schema): make SQL migrations the only schema authority"
```

---

### Task 4: 迁移 repository 中直连 sqlite 的测试

**Files:**
- Modify（各文件中 `gorm.Open(sqlite.Open(...))` 那处建库代码）：
  - `backend/internal/repository/setting_read_test.go:27`
  - `backend/internal/repository/tags_failclosed_test.go:20`
  - `backend/internal/repository/is_admin_test.go:27`
  - `backend/internal/repository/prod_interceptions_test.go:26`
  - `backend/internal/repository/level_test.go:23`
  - `backend/internal/repository/sqlreview_seed_test.go:26`
  - `backend/internal/repository/envtier_clone_test.go:17`
  - `backend/internal/repository/steps_batch_test.go:24`
  - `backend/internal/repository/metadata_search_test.go:27`
  - `backend/internal/repository/exec_window_cas_test.go:18`


**不要改**（这些 sqlite 是**被纳管的目标库**，不是网关自己的存储）：
`internal/gateway/` 下全部测试、`internal/service/exec_batch_test.go`。

**Interfaces:**
- Consumes: `testsupport.NewDB(t)`
- Produces: 无新接口

- [ ] **Step 1: 确认待改清单，排除目标库测试**

```bash
cd backend
grep -rn "sqlite.Open" --include="*_test.go" internal/repository internal/bootstrap
grep -rln "sqlite" --include="*_test.go" internal/gateway internal/service
```
Expected: 第一条只剩 `internal/repository` 下的 10 处（bootstrap 的两处已在 Task 2
改掉）；第二条列出的文件**一个都不改**。

- [ ] **Step 2: 逐个替换建库代码**

每处形如

```go
	db, err := gorm.Open(sqlite.Open(path), &gorm.Config{Logger: gormlogger.Default.LogMode(gormlogger.Silent)})
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	if err := db.AutoMigrate(&model.X{}, &model.Y{}); err != nil {
		t.Fatalf("migrate: %v", err)
	}
```

替换为

```go
	db := testsupport.NewDB(t)
```

同时删掉该文件里因此不再使用的导入（`gorm.io/gorm`、`glebarez/sqlite`、
`gormlogger`、`path`/`t.TempDir()` 相关），加上
`"velagateway/internal/testsupport"`。

- [ ] **Step 3: 跑这两个包**

```bash
cd backend && go test ./internal/repository/ ./internal/bootstrap/ 2>&1 | tail -30
```
Expected: 编译通过。仍失败的用例应只剩大小写（Task 5）与审计链（Task 6）两类，
其余必须全绿。若出现第三类失败，停下来在报告里说明，不要顺手改掉。

- [ ] **Step 4: 确认 sqlite 依赖只剩目标库那一处**

```bash
cd backend && grep -rn "glebarez/sqlite" --include="*.go" . | grep -v "^./internal/gateway"
```
Expected: 无输出（`glebarez/go-sqlite` 是另一个包，供 realdb.go 用，会保留）

- [ ] **Step 5: 提交**

```bash
cd backend && git add internal/repository
git commit -m "test: move repository tests off sqlite onto postgres"
```

---

### Task 5: 修复排序规则导致的登录大小写回归

MySQL 的 `utf8mb4_unicode_ci` 不区分大小写，PG 区分。`admin.go:767` 建用户时已
`ToLower` 入库，但 `repository.go:54` 查询时没有——在 MySQL 下输入
`Linwei@Vela.io` 能登入，换到 PG 会失败。

**Files:**
- Modify: `backend/internal/repository/repository.go:54`
- Test: `backend/internal/repository/is_admin_test.go`（或新建 `user_lookup_test.go`）

**Interfaces:**
- Consumes: Task 1 的 `idx_user_email` 函数索引、`testsupport.NewDB(t)`
- Produces: 无签名变化

- [ ] **Step 1: 写失败的测试**

```go
// 邮箱查询不分大小写。
//
// MySQL 的 utf8mb4_unicode_ci 白送了这个行为,迁到 PG 之后它没了:入库时
// admin.go 会 ToLower,查询时不会,于是「用注册时那个大写写法登录」会失败,
// 而且失败得毫无线索 —— 账号明明在库里。
func TestGetUserByEmail_IsCaseInsensitive(t *testing.T) {
	db := testsupport.NewDB(t)
	r := New(db)

	if err := db.Exec(`INSERT INTO tbl_user (name, email, role_id, status)
	                   VALUES ('林薇', 'linwei@vela.io', 1, 'active')`).Error; err != nil {
		t.Fatalf("seed user: %v", err)
	}

	for _, in := range []string{"linwei@vela.io", "Linwei@Vela.io", "LINWEI@VELA.IO"} {
		u, err := r.GetUserByEmail(in)
		if err != nil {
			t.Errorf("GetUserByEmail(%q): %v", in, err)
			continue
		}
		if u.Email != "linwei@vela.io" {
			t.Errorf("GetUserByEmail(%q) = %q", in, u.Email)
		}
	}
}
```

被测方法是 `(*Repo).GetUserByEmail`，定义在 `repository.go:53`。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && go test ./internal/repository/ -run TestGetUserByEmail_IsCaseInsensitive -v`
Expected: FAIL，大写变体报 record not found

- [ ] **Step 3: 改查询**

`repository.go:54`（`GetUserByEmail` 体内）：

```go
	// lower() 两边都套,才能命中 baseline 里 lower(email) 上的那个唯一索引。
	// 入库侧已经 ToLower(admin.go),但历史行和外部导入不保证,所以查询侧也折一次。
	if err := r.db.Where("lower(email) = lower(?)", email).First(&u).Error; err != nil {
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && go test ./internal/repository/ -run TestGetUserByEmail_IsCaseInsensitive -v`
Expected: PASS

- [ ] **Step 5: 排查其它依赖 ci 语义的判等查询**

```bash
cd backend
grep -rnE 'Where\("(code|engine|status|tier_code|env)' --include="*.go" internal/repository internal/service | grep -v _test
```
逐条判断：值是否由系统写入并受控（如 `status` 只取 `pending`/`approved` 等枚举）。
受控的不改。若发现某列接受用户或 CSV 导入的自由文本，在报告中列出并说明，
**不要顺手改**——那是独立的一次决定。

- [ ] **Step 6: 提交**

```bash
cd backend && git add internal/repository/
git commit -m "fix(auth): keep email lookup case-insensitive on postgres"
```

---

### Task 6: 审计链哈希与时间类型解耦

`audit_chain.go:57` 用 `a.OccurredAt.Format(time.RFC3339)` 喂哈希，该格式带时区
偏移。pgx 读回的 `time.Time` 挂的 Location 取决于会话时区与列类型，写入时按
`+08:00` 算出的哈希，读侧若得到 `...Z` 形式就对不上，**每次链校验都会失败**。

**Files:**
- Modify: `backend/internal/service/audit_chain.go:55-73`、`currentAuditPayloadVersion` 常量
- Test: `backend/internal/service/audit_chain_test.go`（若不存在则新建）

**Interfaces:**
- Consumes: `testsupport.NewDB(t)`
- Produces: `currentAuditPayloadVersion = 5`；`auditPayloadFor(a, 5)` 的 `time`
  字段为 `a.OccurredAt.UTC().Format(time.RFC3339)`

- [ ] **Step 1: 写失败的测试**

```go
// 同一时刻,不同 Location,必须算出同一个哈希。
//
// 这正是 PG 上会踩的那一脚:写入时 time.Now() 带着本地 +08:00,读回来挂的是
// 会话时区(pgx 读 TIMESTAMPTZ)或 UTC(读 TIMESTAMP),RFC3339 字符串一变,
// 重算的哈希就跟库里存的对不上,于是每一次链校验都报「链断了」—— 而链其实是好的。
func TestAuditPayload_IsTimezoneIndependent(t *testing.T) {
	inShanghai, err := time.LoadLocation("Asia/Shanghai")
	if err != nil {
		t.Fatalf("load location: %v", err)
	}
	moment := time.Date(2026, 9, 15, 7, 24, 0, 0, time.UTC)

	a := &model.AuditLog{
		OccurredAt: moment.In(inShanghai),
		ActorName:  "林薇", Instance: "prod-1", Command: "select 1",
		Risk: "low", Result: "ok", ApprovalNo: "AP-1",
	}
	b := &model.AuditLog{
		OccurredAt: moment.In(time.UTC),
		ActorName:  "林薇", Instance: "prod-1", Command: "select 1",
		Risk: "low", Result: "ok", ApprovalNo: "AP-1",
	}

	if got, want := string(auditPayload(b)), string(auditPayload(a)); got != want {
		t.Errorf("同一时刻两种时区算出不同 payload:\n +08:00 → %s\n UTC    → %s", want, got)
	}
}

// 版本机制没被绕过:v5 就是当前版,读侧逐版尝试能认出它。
func TestMatchAuditRow_RecognisesV5(t *testing.T) {
	if currentAuditPayloadVersion != 5 {
		t.Fatalf("currentAuditPayloadVersion = %d, want 5", currentAuditPayloadVersion)
	}
	a := &model.AuditLog{
		OccurredAt: time.Now(), ActorName: "林薇", Instance: "prod-1",
		Command: "select 1", Risk: "low", Result: "ok", ApprovalNo: "AP-1",
		Database: "shop", Operator: "linwei", Env: "prod", TierCode: "t1",
	}
	a.Hash = crypto.ChainHash("", auditPayload(a))
	if v := matchAuditRow("", a); v != 5 {
		t.Fatalf("matchAuditRow = %d, want 5", v)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && go test ./internal/service/ -run "TestAuditPayload_IsTimezoneIndependent|TestMatchAuditRow_RecognisesV5" -v`
Expected: 两个都 FAIL（payload 不一致；版本仍是 4）

- [ ] **Step 3: 改 payload 并升版**

`audit_chain.go`：

```go
	m := map[string]any{
		// UTC 归一 —— 哈希不能跟着读回来的 Location 走。存进 TIMESTAMPTZ 的是
		// 绝对时刻,取出来挂什么时区由会话决定,而链的定义必须只认那个时刻本身。
		"time": a.OccurredAt.UTC().Format(time.RFC3339), "actor": a.ActorName,
		...
	}
```

`currentAuditPayloadVersion` 由 4 改为 5。

时间规范化对每一版都生效（不放在 `if version >= 5` 里）：版本号在这里标记的是
**算法变更**而非字段增补，读侧 `matchAuditRow` 从新到旧试，新库里的行一次命中 v5。

`auditPayloadFor` 上方补一行注释说明 v5 是什么：

```go
// v5:time 改为 UTC 归一(迁 PostgreSQL 时的时区无关化)。
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && go test ./internal/service/ -run "TestAuditPayload_|TestMatchAuditRow_" -v`
Expected: PASS

- [ ] **Step 5: 跑整个 service 包，确认既有链测试没被打破**

```bash
cd backend && go test ./internal/service/ 2>&1 | tail -20
```
Expected: 全绿

- [ ] **Step 6: 提交**

```bash
cd backend && git add internal/service/
git commit -m "fix(audit): normalise chain hash timestamps to UTC"
```

---

### Task 7: 外围收尾

**Files:**
- Create: `backend/run-dev.bat`, `backend/run-dev.sh`
- Delete: `backend/run-sqlite.bat`, `backend/run-mysql.bat`
- Delete: `backend/vela-gateway.db`, `backend/vela-gateway.db-shm`, `backend/vela-gateway.db-wal`
- Modify: `docker-compose.yml`
- Modify: `backend/internal/model/model.go`（8 处 `type:mediumtext`、2 处 `type:char(64)`）
- Modify: `README.md`, `DEPLOY.md`, `CLAUDE.md`
- Create: `docs/adr/0008-postgres-single-store.md`

**不要动** `backend/target.db`（演示目标库数据）。

**Interfaces:**
- Consumes: 前 6 个任务
- Produces: 无

- [ ] **Step 1: 清理 model 里的 MySQL 专用类型标记**

```bash
cd backend
sed -i '' 's/type:mediumtext/type:text/g' internal/model/model.go
sed -i '' 's/type:char(64)/type:varchar(64)/g' internal/model/model.go
grep -c "type:mediumtext\|type:char(64)" internal/model/model.go
```
Expected: `0`。PG 没有长度分级的 TEXT 变体；`char(n)` 的 blank-padded 语义会给
`prev_hash` 的 Go 侧字符串判等埋雷。

- [ ] **Step 2: 启动脚本**

删两个旧的，写 `backend/run-dev.bat`：

```bat
@echo off
REM 本地启动 AegisDB 后端(PostgreSQL)。
REM 前置:装好 PostgreSQL 并建库  createdb vela_gateway
REM DSN 见 configs/config.yaml 的 postgres_dsn(可用 VELA_PG_DSN 覆盖)。
setlocal
set APP_ENV=dev

set GO=go
where go >nul 2>nul || set GO="C:\Program Files\Go\bin\go.exe"

echo [AegisDB] env=dev (postgres)  ->  http://localhost:8080
echo 演示登录: linwei@vela.io / vela123
%GO% run ./cmd/server
endlocal
```

和 `backend/run-dev.sh`（开发机是 macOS）：

```sh
#!/bin/sh
# 本地启动 AegisDB 后端(PostgreSQL)。
# 前置:装好 PostgreSQL 并建库  createdb vela_gateway
set -e
cd "$(dirname "$0")"
export APP_ENV=dev
echo "[AegisDB] env=dev (postgres)  ->  http://localhost:8080"
echo "演示登录: linwei@vela.io / vela123"
exec go run ./cmd/server
```

```bash
cd backend && git rm run-sqlite.bat run-mysql.bat
git rm --cached vela-gateway.db vela-gateway.db-shm vela-gateway.db-wal 2>/dev/null || true
rm -f vela-gateway.db vela-gateway.db-shm vela-gateway.db-wal
chmod +x run-dev.sh
```

- [ ] **Step 3: docker-compose.yml**

`mysql` 服务整段换成：

```yaml
  postgres:
    image: postgres:16
    container_name: vela-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: vela_gateway
      POSTGRES_USER: vela
      POSTGRES_PASSWORD: velapass
      TZ: Asia/Shanghai
    ports:
      - "127.0.0.1:5432:5432" # bind to loopback only — not the host's public interfaces
    volumes:
      - vela_pg:/var/lib/postgresql/data
      # Schema is applied by the app, not initdb.d, so it is tracked in
      # schema_migrations and there is a single migration source of truth:
      #   VELA_PG_DSN=... ./vela-gateway migrate   (then `init` for seed+admin)
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vela -d vela_gateway"]
      interval: 5s
      timeout: 3s
      retries: 20
```

`volumes:` 段 `vela_mysql:` 改为 `vela_pg:`，Redis 保留。文件顶部注释里的
「MySQL 8 + Redis 7」改为「PostgreSQL 16 + Redis 7」，并加一句说明：本机已装
PostgreSQL 时不需要这个文件。

- [ ] **Step 4: 文档**

```bash
cd /Users/abiu/GolandProjects/project/db-gateway
grep -n "MySQL\|mysql\|SQLite\|sqlite\|run-sqlite\|run-mysql" README.md DEPLOY.md CLAUDE.md
```

逐处修改。要点：

- README 的「零依赖启动」改为「需要本机 PostgreSQL」，双击目标从
  `run-sqlite.bat` 改为 `run-dev.bat`，补 `createdb vela_gateway` 这一步
- DEPLOY.md 的 MySQL 部署段改为 PostgreSQL，`VELA_MYSQL_DSN` → `VELA_PG_DSN`
- CLAUDE.md 那一处「Go · Gin · GORM · MySQL/SQLite」改为「Go · Gin · GORM · PostgreSQL」，
  并把本地启动说明里的 `run-sqlite.bat` 改为 `run-dev.bat`
- 补一句：跑测试需要 `createdb vela_test`

**注意**：README 里描述「网关能纳管哪些数据库」的地方提到 MySQL/TiDB 是**正确的**，
不要改——那是目标库能力。只改描述网关自身存储的句子。

- [ ] **Step 5: 写 ADR**

`docs/adr/0008-postgres-single-store.md`。先看 `docs/adr/0007-sql-review-from-specs.md`
的格式并沿用。内容要点：双 schema 权威源导致过 tier_code 事故（db.go 有记录）；
决定合并到 PostgreSQL 单一存储；代价是测试不再零依赖；目标库驱动不受影响。

- [ ] **Step 6: 全量验证**

```bash
cd backend
go build ./... && echo BUILD_OK
go vet ./... 2>&1 | tail -5
go test ./... 2>&1 | tail -30
```
Expected: `BUILD_OK`；vet 干净；测试全绿

- [ ] **Step 7: 确认没有残留**

```bash
cd backend
grep -rn "sqlite\|mysql\|MySQL\|SQLite" internal/bootstrap migrations configs cmd
```
Expected: 无输出。（`internal/gateway/` 不在范围内，那里的 MySQL/SQLite 是目标库驱动。）

- [ ] **Step 8: 提交**

```bash
cd /Users/abiu/GolandProjects/project/db-gateway
git add -A
git commit -m "chore: retire MySQL/SQLite from the gateway store's tooling and docs"
```

---

### Task 8: 真实跑一遍

计划的最后一步不是测试绿，是应用真的能用。

**Files:** 无改动（发现问题则回到对应任务修）

- [ ] **Step 1: 重建库并初始化**

```bash
cd backend
dropdb --if-exists vela_gateway && createdb vela_gateway
go run ./cmd/server init --config configs/config.yaml
```
Expected: 无报错

- [ ] **Step 2: 启动后端**

```bash
cd backend && ./run-dev.sh
```
Expected: 监听 `:8080`，日志无 error

- [ ] **Step 3: 按 run-app skill 驱动前端走一遍**

用 `run-app` skill 启动前端并点击验证：

1. 用 `linwei@vela.io / vela123` 登录 → 成功
2. 用 `Linwei@Vela.io`（大写变体）登录 → 同样成功（Task 5 的验收）
3. 新建一个数据库连接 → 保存成功
4. 发起一次审批 → 审批链正常显示
5. 执行一次 SQL → 有结果返回
6. 打开审计页 → 触发链校验 → 报告 OK（Task 6 的验收）

- [ ] **Step 4: 记录结果**

把 6 项逐条的实际结果写进报告。有失败的，指出是哪个任务的缺口，不要就地打补丁。
